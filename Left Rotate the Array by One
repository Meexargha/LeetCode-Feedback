#include <bits/stdc++.h> 
vector<int> rotateArray(vector<int>& arr, int n) {
    // Write your code here.
    for(int i=0;i<n-1;i++){
        int temp= arr[i];
        arr[i]=arr[i+1];
        arr[i+1]=temp;
    }
    return arr;
}
